<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gaorui.dao.NodeDao">
  
    <!-- id是唯一标识符，与Mapper接口的方法名保持一致，参数类型parameterType是参数类型的全限定名，这里使用的是别名 -->  
    <select id="ShowNode" resultType="com.gaorui.bean.NodeBean">
    SELECT  nid,title,description,pic,topics,create_time,update_time,is_del FROM t_node ;
    </select>


</mapper>  